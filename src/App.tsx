import React, { FC } from 'react';

import Routes from './Routes';
import './App.scss';

const App: FC = () => {
  return (
    <div id="App">
      <Routes />
    </div>
  );
};

export default App;
